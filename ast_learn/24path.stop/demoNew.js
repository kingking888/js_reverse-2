function hello(n) {
  console.log(n);
}

let obj = {
  name: 'xiaojianbang',
  // 这是干啥
  add: function (x, b) {
    var x = "";
    return x + b + 1000;
  },
  mul: function (x, b) {
    var b = "";
    return x * b + 1000;
  }
};